#pragma once
#include <iostream>
using namespace std;
#include <stack>
#include <queue>
#include <deque>
void main_stack();
void main_queue();
void main_deque();
void type_stack(std::stack <double> _stack);
void type_queue(std::queue <double> _queue);
void type_deque(std::deque <double> _queue);